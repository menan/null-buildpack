#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

AWESOME_VM_BINARY="https://s3.amazonaws.com/menan/taglib/taglib-1.8.gz"
AWESOME_VM_VENDOR="vendor/taglib-1.8"

# vendor taglib
mkdir -p $1/$AWESOME_VM_VENDOR
curl $AWESOME_VM_BINARY -o - | tar -xvf -C $1/$AWESOME_VM_VENDOR -f -
